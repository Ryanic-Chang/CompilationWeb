<div align="center">
  <img src="./logo.svg" alt="Compilation-Web Logo" width="100" height="100" />
  <h1>编译原理实验平台<br>(Compilation Web)</h1>
  <p><b>Compilation Web</b> 是一套风格明亮、现代化的编译原理实验网页版平台。<br/>旨在将传统的编译原理实验（如 DFA 模拟、词法分析、LR(0)、SLR(1) 语法分析、语义分析、AST 可视化、中间代码生成、内存地址映射与目标代码生成）通过纯前端方式直接搬到网页端，提升学习体验、交互性和对算法的理解。</p>
</div>

<p align="center">
  <a href="https://ryanic-chang.github.io/CompilationWeb/" target="_blank">
    <img src="https://img.shields.io/badge/🚀_在线体验-点击访问-3B82F6?style=plastic&labelColor=38BDF8&color=3B82F6" alt="在线体验">
  </a>
</p>

---

欢迎点点star⭐~

## ✨ 特性

- **现代化 UI**: 基于 HTML5 + Tailwind CSS (CDN) 构建，界面明亮、结构清晰的响应式设计。
- **纯前端架构**: 无需复杂的后端或 Node.js 构建工具，纯浏览器原生运行，克隆即用。
- **动态可视化**: 借助 `vis-network` 图形库，将晦涩难懂的状态转移、SLR 规范族、语法树等数据结构转化为直观的动态交互图。
- **两级页面结构**: 新增独立的 `overview.html` 作为实验总览页，用于展示全局概况、进度统计、完整流水线与实验入口；`index.html` 作为二级实验页承载各实验的输入、执行与结果展示。
- **中间代码输出**: 支持基于 AST 自动生成三地址码与四元式，便于展示编译前端到中间表示的完整流程。
- **目标代码生成**: 新增实验七&八，可继续生成优化后四元式、内存地址映射以及可下载的 `x86` / `arm64` 汇编文件。
- **运行时配套**: 页面内置并可下载 `runtime.c`，同时给出建议编译命令，减少手动拼接汇编与运行时文件的麻烦。
- **高可扩展性**: 侧边栏支持多级网页导航，预留了多个实验的插槽，方便后续以模块化的形式添加新实验（如 `ll1.js`, `lr1.js`）。
- **人性化输入**: 支持直接手写、结构化表单输入或通过 `txt` 文件一键上传（自动解析与智能回填表单），极大简化了测试数据的准备。

## 🚀 快速开始

本项目为纯静态前端页面，运行非常简单：

1. 克隆或下载本仓库到本地：
   ```bash
   git clone https://github.com/your-username/compilation-web.git
   ```
2. 推荐优先打开 `overview.html` 进入独立实验总览页；也可以直接打开 `index.html` 进入二级实验页。
3. （可选）如果您需要使用 VS Code 进行二次开发，推荐安装 `Live Server` 插件，并在项目根目录启动服务，以获得更佳的热更新体验。

### 页面入口

- 总览页：`overview.html`
- 实验页：`index.html`
- 在线访问实验页主页：[https://ryanic-chang.github.io/CompilationWeb/](https://ryanic-chang.github.io/CompilationWeb/)
- 在线访问总览页：[https://ryanic-chang.github.io/CompilationWeb/overview.html](https://ryanic-chang.github.io/CompilationWeb/overview.html)

## 📂 项目结构

```text
CompilationWeb/
├── overview.html   # 独立实验总览页：展示全局概况、进度统计、完整流水线与实验入口
├── index.html      # 二级实验页：包含侧边栏导航和所有实验模块的 DOM 结构
├── style.css       # 样式文件：页面切换动画与定制 UI 细节
├── app.js          # 核心交互：控制多级网页的切换与通用 UI 逻辑
├── overview.js     # 总览页逻辑：实验状态读取、统计更新、节点状态同步与跳转逻辑
├── dfa.js          # 实验一模块：DFA 核心类（解析、校验、生成字符串）及专属 UI 事件
├── scanner.js      # 实验二模块：词法分析器 (Scanner) 引擎、DFA 图表及表格渲染逻辑
├── lr0.js          # 实验三模块：LR(0) 语法分析核心引擎（闭包、GoTo、冲突检测）及 UI 事件
├── slr1.js         # 实验四模块：SLR(1) 语法分析核心引擎（FIRST/FOLLOW 集、分析表构建）及 UI 事件
├── semantic.js     # 实验五模块：语义分析、符号表构建、AST 生成与可视化逻辑
├── exp6.js         # 实验六模块：中间代码生成（三地址码、四元式）及专属 UI 事件
├── exp78.js        # 实验七&八模块：四元式优化、内存地址映射、x86/arm64 汇编生成、下载与内置运行时模板
├── mainGGG.txt     # 实验五~实验八默认文法：前端语义与后端模块自动载入的主文法
└── .trae/          # AI 辅助开发目录：存放项目专属开发规范 Skill
```

## 🛠️ 当前与后续开发计划

- [x] **实验一：DFA 模拟与可视化**
  - 五元组解析与合法性验证
  - 输入字符串匹配
  - 生成指定长度内的所有接受字符串
  - 自动渲染状态转移图
- [x] **实验二：词法分析 (Scanner)**
  - 对齐最新版 `scanner.cpp`：支持关键字、标识符、整数/浮点数、字符串、关系/逻辑运算符、界符
  - 支持 `//` 单行与 `/* */` 多行注释过滤
  - 新增 `for` 关键字识别与全角分号错误提示
  - 交互式的 Token 流表格与导出结果功能
  - 绘制并展示主文法的 0~22 状态机 DFA 图及详细状态转换表
- [x] **实验三：LR(0) 语法分析**
  - 支持 `->` 产生式定义及 `|` 备选项分隔解析
  - 自动对文法进行增广处理（生成 `S' -> S`）
  - 核心算法实现：项目集闭包 (`Closure`) 与状态转移 (`GoTo`)
  - 移进-归约 / 归约-归约 冲突自动检测
  - 项目集规范族生成及文本格式导出
  - 可视化转移关系图（Box 形态，区分内核项与闭包项）
- [x] **实验四：SLR(1) 语法分析**
  - 基于 LR(0) 项目集规范族自动计算 `FIRST 集` 和 `FOLLOW 集`
  - 自动构建包含 `ACTION` 和 `GOTO` 的 SLR(1) 状态转移分析表
  - 精准检测移进-归约与归约-归约冲突，并在分析表中高亮标注
  - 交互式表格视图，提供集合与规范族文本导出功能
- [x] **实验五：语义分析与 AST 可视化**
  - 支持默认载入或手动上传 `mainGGG.txt` 文法文件
  - 支持手动输入或上传源代码文件，并复用实验二词法分析器生成 Token 流
  - 基于 SLR 分析表执行语义动作，构建符号表与抽象语法树
  - 支持函数参数检查、重复声明检查、未声明变量检查与返回类型检查
  - 提供符号表、语义错误、AST 文本、AST 图、Token 流五类结果面板
- [x] **实验六：中间代码生成**
  - 支持默认载入或手动上传 `mainGGG.txt` 文法文件
  - 支持手动输入或上传源代码文件，并复用实验五的 AST 与语义分析结果
  - 基于 AST 生成三地址码（Three-Address Code）
  - 同步输出四元式（Quadruples）表示
  - 结果面板仅保留 AST、语义错误报告、三地址码、四元式四类核心输出
- [x] **实验七&八：内存地址映射与目标代码生成**
  - 支持默认载入或手动上传 `mainGGG.txt` 文法文件
  - 支持手动输入或上传源代码文件，并以 `test.c` 一类源码直接驱动后端实验流程
  - 对实验六生成的四元式继续执行简单优化，并展示优化后结果
  - 自动构建函数栈帧、局部变量区、参数区、临时变量区的内存地址映射
  - 自动生成 `x86` 与 `arm64` 两份目标汇编代码，并支持一键下载全部结果
  - 页面直接展示建议编译命令，同时提供内置 `runtime.c` 模板下载；若项目根目录存在本地 `runtime.c`，则优先读取本地文件内容

## 🧪 实验七&八使用说明

1. 打开左侧导航中的“实验七&八: 目标代码”。
2. 保持默认 `mainGGG.txt`，并输入或上传 `test.c` 这类源代码文件。
3. 点击“执行实验七&八”后，可在右侧查看：
   - 中间代码
   - 内存地址映射
   - `x86` 汇编
   - `arm64` 汇编
   - `runtime.c`
4. 使用顶部按钮可分别下载，或通过“一键下载全部”一次性下载三份核心结果：
   - `X_x86.s`
   - `X_arm64.s`
   - `X_runtime.c`
5. 页面还会自动给出建议编译命令，便于后续本地联调。

示例命令：

```bash
gcc -m32 test_x86.s test_runtime.c -o test_x86
aarch64-linux-gnu-gcc test_arm64.s test_runtime.c -o test_arm64
```

## 🗺️ 页面规划

- `overview.html` 为一级页面，只负责信息总览、整体进度、完整流程节点和实验入口，不承载实验输入与运行逻辑。
- `index.html` 为二级页面，保留实验一到实验八的输入区、执行按钮、图形展示和结果面板。
- 从总览页进入实验页时，会以新标签页打开对应实验模块，方便保留总览页作为导航主页。
- 实验页顶部提供返回总览页入口，便于在总览与实验之间来回切换。

## 🤖 AI 辅助开发指南 (Skill)

为了保证后续通过 AI (如 Trae 等) 持续开发本项目时，能够维持一致的代码风格、架构和 UI 规范，我们在 `.trae/skills/compilation-web-dev/SKILL.md` 中预设了项目专用的 AI 开发规范 (Skill)。
AI 在接收到新实验开发需求时，会自动读取该文件以确保新模块能够完美融入当前架构。

## 📄 开源协议

本项目采用 [MIT License](LICENSE) 开源，欢迎提交 Pull Request 和 Issue 一起完善编译原理的实验体验！
